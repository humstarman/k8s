kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  namespace: datacentre
  name: glusterfs-cassandra-for-worker
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
